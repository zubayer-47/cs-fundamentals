#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    // O(sqrt(n))
    for (int i=0; sqrt(n) <n; i++) {

    }

    // O(sqrt(n))
    for (int i=0; i*i < n; i++) {

    }

    return 0;
}