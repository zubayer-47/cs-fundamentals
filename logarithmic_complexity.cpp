#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    // log2 n
    for (int i=0; i<n; i *= 2) {

    }

    // log3 n (if * or / in the increment part then it will count as logarithmic complexity)
    for (int i=0; i<n; i *= 3) {

    }

    return 0;
}